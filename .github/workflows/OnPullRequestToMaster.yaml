name: OpenApi Checks

on:
  pull_request:
    branches:
      - master

jobs:
  checks:    
    uses: TechnicalFreak/fop.openapi-rules/.github/workflows/check-openapi.yaml@11
    with:
      rule-version: 11
      openapi-file-path: 'my-api.yaml'


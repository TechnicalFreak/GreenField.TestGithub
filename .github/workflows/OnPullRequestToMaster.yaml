name: OpenApi Checks

on:
  pull_request:
    branches:
      - master

jobs:
  checks:    
    uses: TechnicalFreak/fop.openapi-rules/.github/workflows/check-openapi.yaml@12
    with:
      rule-version: 12
      openapi-file-path: 'my-api.yaml'


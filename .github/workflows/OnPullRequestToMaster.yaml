name: OpenApi Checks

on:
  pull_request:
    branches:
      - master

jobs:
  checks:    
    uses: TechnicalFreak/fop.openapi-rules/.github/workflows/check-openapi.yaml@6
    with:
      rule-version: 6
      openapi-file-path: 'my-api.yaml'


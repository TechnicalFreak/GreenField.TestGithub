name: OpenApi Checks

on:
  pull_request:
    branches:
      - master

jobs:
  checks:    
    uses: TechnicalFreak/fop.openapi-rules/.github/workflows/check-openapi.yaml@5
    with:
      rule-version: 5
      openapi-file-path: 'my-api.yaml'


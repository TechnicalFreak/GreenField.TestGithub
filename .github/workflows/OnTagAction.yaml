on:
  push:
    branches:
      - master
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  Release:
    if: github.event_name == 'push' && github.event.ref_type == 'tag' && github.ref == 'refs/tags/v[0-9]+.[0-9]+.[0-9]+'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello Frank"

name: TestAction

on:
  push:
    branches:
      - master
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  Release:    
    runs-on: ubuntu-latest
    steps:      
      - name: Print Variables
        run: |
          echo "GitHub Event Name: $GITHUB_EVENT_NAME"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "SHA: $GITHUB_SHA"
          echo "Ref: $GITHUB_REF"
      - run: echo "Hello Frank"